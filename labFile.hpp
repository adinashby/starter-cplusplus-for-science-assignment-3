/* ONLY WRITE THE ANSWER TO THE QUESTIONS IN THE CORRESPONDING METHODS */

#ifndef LABFILE_HPP
#define LABFILE_HPP

#include <iostream>
#include <vector>

using namespace std;

vector<int> createAndRotateArray(int size, int startingInteger) {
    return vector<int>();
}

bool isMagicSquare(const std::vector<std::vector<int>>& matrix) {
    return false;
}

vector<vector<int>> generateMagicSquare(int n) {
    return vector<vector<int>>();
}

#endif
